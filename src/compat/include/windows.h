// compat/include/windows.h
// Linux-заглушка для `#include <windows.h>` в исходниках SFILE95.
// Реальных вызовов win32-API (CreateFile, GetLastError и т.п.) в этих файлах
// нет. Однако S_wemt.cpp использует базовые win-типы BOOL/TRUE/FALSE (не
// API-вызовы, а просто типы) — они уже определены в компат-шиме tmc_mfc.h
// (используется и для <afx.h>), поэтому подключаем его и здесь, не дублируя
// определения. На Windows подключается настоящий windows.h (этот файл
// используется только вне WIN32, см. src/CMakeLists.txt: tmc_compat
// подключается только при NOT WIN32).
// Если когда-либо обнаружится реальный вызов win32-API — заглушку расширять
// нельзя без анализа математики/совместимости, нужно эскалировать.
#pragma once
#include "tmc_mfc.h"
