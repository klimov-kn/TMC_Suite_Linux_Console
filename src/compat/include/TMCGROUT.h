// Пустая заглушка <TMCGROUT.h> для сборки под Linux.
//
// Файлы S-матрицы viewers/Tmcrtout/TmcTtoS.cpp и TmcSMatrix.cpp включают
// "TMCGROUT.h" (заголовок viewer-проекта графического вывода), но НИ ОДИН символ
// из него не используют — единственное упоминание «TMCGROUT» в обоих файлах это
// сама строка #include (проверено grep). Заголовок не был импортирован в win_src
// (это часть невнесённого GUI-вьюера). Поскольку он «мёртвый» для этих TU, пустая
// заглушка полностью эквивалентна по поведению (тот же приём, что для conio.h/io.h,
// см. ISSUES.md X1/S2). Подключается через INTERFACE-цель tmc_compat (NOT WIN32).
#pragma once
